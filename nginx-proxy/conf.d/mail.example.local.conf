server {

    listen 80;
    listen [::]:80;

    server_name mail.example.local;

    location / {

        proxy_pass http://172.22.0.2:3000;
        proxy_buffering off;
        proxy_set_header X-Real-IP $remote_addr;
        
    }
}